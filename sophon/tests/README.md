# Sophon Tests

Sophon test suites should live here and stay scoped to `sophon/src`.

Suggested first suites:

- priority scoring unit tests
- adaptive focus-count tests (3-7 range)
- action policy routing tests (auto/approve/suggest)
- staleness ladder transition tests
