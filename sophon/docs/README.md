# Sophon Docs

This folder is for Sophon-specific documentation that should not live in Virgil runtime docs.

## Suggested files

- `architecture.md` for module boundaries and data flow.
- `integration.md` for Virgil-to-Sophon touchpoints.
- `roadmap.md` for staged delivery milestones.

## Source of truth (current)

Until these docs are split out, use:

- `docs/superpowers/specs/2026-04-05-sophon-daily-command-center-design.md`
